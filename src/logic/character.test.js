describe('Character', () => {
  describe('has the following initial properties:', () => {
    it('allowedTiers');
  });
  describe('throws error if argument', () => {
    it('name is not a string');
    it('tier is not a string from allowedTiers');
    it('image is not a link');
  });
});
